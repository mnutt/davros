{{#paper-list}}
  {{#paper-item onClick=(transition-to "home")}}
    {{paper-icon "folder"}}
    Files
  {{/paper-item}}

  {{#if canSync}}
    {{#paper-item onClick=(transition-to "clients")}}
      {{paper-icon "desktop-mac"}}
      Clients
    {{/paper-item}}
  {{/if}}

  {{#if canEdit}}
    {{#paper-item onClick=(transition-to "publishing")}}
      {{paper-icon "web"}}
      Publishing
    {{/paper-item}}
  {{/if}}

  {{#paper-item onClick=(transition-to "about")}}
    {{paper-icon "info-outline"}}
    About
  {{/paper-item}}

{{/paper-list}}
