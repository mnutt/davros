<iframe title="offer iframe"
        class="offer-iframe border h-8 p-1 bg-white"
        src={{this.src}}
        ...attributes
        {{on-insert this.setupListener}}
        {{on-destroy this.destroyListener}}>
</iframe>
